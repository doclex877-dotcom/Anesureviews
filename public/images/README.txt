Save each downloaded image here using the EXACT filename listed below.
These filenames are already wired into src/lib/articles.ts (heroImage field) —
once the file exists at this path, it will automatically appear on the
homepage card, the guides listing card, and the top of the article page.

See the image shopping list the assistant provided in chat for search terms.
